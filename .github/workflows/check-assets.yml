name: Validate image assets
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: image-check-${{ github.ref }}
  cancel-in-progress: true
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - run: python -m pip install pillow certifi
      # The daily sync runs these too, but only after merge; fail the PR instead.
      - run: python -m unittest discover -s scripts -p 'test_*.py'
      - run: python scripts/validate_images.py
